import { GitlabOutlined } from "@ant-design/icons";
import styled from "@emotion/styled";

const MyIcon = styled(GitlabOutlined)`
  font-size: 100ox;
  color: orange;
`;

export default function LibraryIconPage() {
  return <MyIcon />;
}
