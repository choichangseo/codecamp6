import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: greenyellow;
`;

export default function LayoutHeader(props) {
  return <Wrapper>English</Wrapper>;
}
