import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 100px;
  background-color: hotpink;
`;

export default function LayoutFooter(props) {
  return <Wrapper>여기는 footer 입니다.</Wrapper>;
}
