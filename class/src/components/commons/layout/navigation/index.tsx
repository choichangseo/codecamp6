import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 100px;
  background-color: purple;
`;

export default function LayoutNavigation(props) {
  return <Wrapper>EnglishNav</Wrapper>;
}
